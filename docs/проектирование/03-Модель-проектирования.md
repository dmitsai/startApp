# Модель проектирования (Design Model)

Модель проектирования описывает внутреннюю структуру системы и взаимодействие компонентов при выполнении операций.

## Системные диаграммы последовательностей

### Операция: Создание проекта

```mermaid
sequenceDiagram
    actor Пользователь
    participant UI as Frontend (Next.js)
    participant API as tRPC Router
    participant Service as ProjectService
    participant DB as Prisma (PostgreSQL)
    
    Пользователь->>UI: Заполняет форму проекта
    Пользователь->>UI: Нажимает "Создать проект"
    UI->>API: createProject(projectData)
    API->>Service: validateProjectData(projectData)
    Service-->>API: validationResult
    
    alt Данные валидны
        API->>Service: createProject(projectData, userId)
        Service->>DB: transaction.begin()
        Service->>DB: project.create()
        Service->>DB: projectTechnology.createMany()
        Service->>DB: projectPassport.create()
        DB-->>Service: project
        Service->>DB: transaction.commit()
        Service-->>API: project
        API-->>UI: success(project)
        UI-->>Пользователь: Проект создан успешно
    else Данные невалидны
        API-->>UI: error(validationErrors)
        UI-->>Пользователь: Ошибка валидации
    end
```

### Операция: Отклик на проект

```mermaid
sequenceDiagram
    actor Участник
    participant UI as Frontend
    participant API as tRPC Router
    participant AppService as ApplicationService
    participant RecService as RecommendationService
    participant DB as Prisma
    
    Участник->>UI: Просматривает проект
    Участник->>UI: Нажимает "Откликнуться"
    UI->>API: createApplication(projectId, message)
    
    API->>AppService: checkExistingApplication(userId, projectId)
    AppService->>DB: application.findFirst()
    DB-->>AppService: existingApplication
    
    alt Отклик уже существует
        AppService-->>API: error("Уже откликались")
        API-->>UI: error
        UI-->>Участник: Вы уже откликались на этот проект
    else Новый отклик
        API->>AppService: createApplication(userId, projectId, message)
        AppService->>DB: application.create()
        DB-->>AppService: application
        
        AppService->>RecService: calculateMatchScore(userId, projectId)
        RecService->>DB: user.findUnique(technologies)
        RecService->>DB: project.findUnique(technologies)
        RecService->>RecService: compareTechnologies()
        RecService->>RecService: calculateScore()
        RecService-->>AppService: matchScore, validity
        
        AppService->>DB: application.update(score, validity)
        AppService-->>API: application
        API-->>UI: success(application)
        UI-->>Участник: Отклик отправлен
    end
```

### Операция: Получение рекомендаций проектов

```mermaid
sequenceDiagram
    actor Пользователь
    participant UI as Frontend
    participant API as tRPC Router
    participant RecService as RecommendationService
    participant Cache as React Query Cache
    participant DB as Prisma
    
    Пользователь->>UI: Открывает "Рекомендации"
    UI->>Cache: Проверяет кэш
    
    alt Данные в кэше
        Cache-->>UI: cachedRecommendations
        UI-->>Пользователь: Отображает рекомендации
    else Данных нет в кэше
        UI->>API: getRecommendations(userId)
        API->>RecService: generateRecommendations(userId)
        
        RecService->>DB: user.findUnique(technologies, cv)
        DB-->>RecService: userProfile
        
        RecService->>DB: project.findMany(active)
        DB-->>RecService: allProjects
        
        loop Для каждого проекта
            RecService->>RecService: calculateRelevanceScore(userProfile, project)
            RecService->>RecService: generateJustification()
        end
        
        RecService->>RecService: sortByScore()
        RecService-->>API: recommendations
        
        API-->>UI: recommendations
        UI->>Cache: Сохраняет в кэш
        UI-->>Пользователь: Отображает рекомендации
    end
```

### Операция: Умный подбор стека для учебного проекта

```mermaid
sequenceDiagram
    actor Создатель
    participant UI as Frontend
    participant API as tRPC Router
    participant StackService as SmartStackService
    participant AppService as ApplicationService
    participant DB as Prisma
    
    Создатель->>UI: Включает "Умный подбор стека"
    Создатель->>UI: Нажимает "Подобрать стек"
    UI->>API: getSmartStack(projectId)
    
    API->>AppService: getApplications(projectId)
    AppService->>DB: application.findMany(projectId, status=accepted)
    DB-->>AppService: applications
    
    AppService->>DB: user.findMany(application.userIds)
    DB-->>AppService: candidates
    
    AppService-->>API: candidates
    
    API->>StackService: analyzeCandidateSkills(candidates)
    
    loop Для каждого кандидата
        StackService->>StackService: extractTechnologies(candidate)
        StackService->>StackService: calculateSkillLevels()
    end
    
    StackService->>StackService: findStrongestTechnologies()
    StackService->>StackService: findLearningTechnologies()
    StackService->>StackService: optimizeStack()
    StackService-->>API: recommendedStack
    
    API-->>UI: recommendedStack
    UI-->>Создатель: Отображает рекомендуемый стек
```

## Диаграмма классов проектирования

```mermaid
classDiagram
    class ProjectController {
        +createProject(data)
        +updateProject(id, data)
        +deleteProject(id)
        +getProject(id)
        +listProjects(filters)
    }
    
    class ApplicationController {
        +createApplication(projectId, message)
        +getApplications(projectId)
        +acceptApplication(id)
        +rejectApplication(id)
    }
    
    class RecommendationController {
        +getRecommendations(userId)
        +calculateMatchScore(userId, projectId)
    }
    
    class SmartStackController {
        +getSmartStack(projectId)
        +analyzeCandidateSkills(candidates)
    }
    
    class ProjectService {
        -prisma: PrismaClient
        +validateProjectData(data)
        +createProject(data, userId)
        +updateProject(id, data)
    }
    
    class ApplicationService {
        -prisma: PrismaClient
        +createApplication(userId, projectId, message)
        +calculateMatchScore(userId, projectId)
    }
    
    class RecommendationService {
        -prisma: PrismaClient
        +generateRecommendations(userId)
        +calculateRelevanceScore(userProfile, project)
    }
    
    class SmartStackService {
        +analyzeCandidateSkills(candidates)
        +findStrongestTechnologies(skills)
        +optimizeStack(technologies)
    }
    
    class PrismaClient {
        +user
        +project
        +application
        +technologyTag
        +userTechnology
        +projectTechnology
    }
    
    ProjectController --> ProjectService
    ApplicationController --> ApplicationService
    ApplicationController --> RecommendationService
    RecommendationController --> RecommendationService
    SmartStackController --> SmartStackService
    SmartStackController --> ApplicationService
    
    ProjectService --> PrismaClient
    ApplicationService --> PrismaClient
    RecommendationService --> PrismaClient
```

## Описание операций

### Операция: createProject

**Входные параметры:**
- `projectData`: {
    - `название`: string
    - `описание`: string
    - `цели`: string
    - `задачи`: string
    - `типПроекта`: "учебный" | "коммерческий"
    - `сроки`: DateTime
    - `технологии`: Array<{id: string, обязательна: boolean, минимальныйГрейд: number}>
    - `роли`: string
    - `требования`: string
  }
- `userId`: string

**Постусловия:**
- Создан новый проект в базе данных
- Проект связан с пользователем (создателем)
- Созданы связи проекта с технологиями
- Создан паспорт проекта
- Проект доступен для просмотра другими пользователями

**Исключения:**
- `ValidationError` — невалидные данные
- `UnauthorizedError` — пользователь не авторизован

---

### Операция: createApplication

**Входные параметры:**
- `projectId`: string
- `message`: string (опционально)
- `userId`: string

**Постусловия:**
- Создан новый отклик в базе данных
- Отклик связан с пользователем и проектом
- Вычислена оценка подходящности
- Определена валидность кандидата
- Создатель проекта уведомлен о новом отклике

**Исключения:**
- `DuplicateApplicationError` — пользователь уже откликался
- `ProjectNotFoundError` — проект не найден

---

### Операция: calculateMatchScore

**Входные параметры:**
- `userId`: string
- `projectId`: string

**Возвращает:**
- `score`: number (0-100) — оценка подходящности
- `validity`: boolean — соответствует ли требованиям

**Алгоритм:**
1. Получить технологии пользователя с грейдами
2. Получить требуемые технологии проекта
3. Для каждой требуемой технологии:
   - Проверить наличие у пользователя
   - Сравнить грейд пользователя с минимальным требуемым
   - Вычислить вклад в общую оценку
4. Учесть обязательные технологии (если отсутствуют — валидность = false)
5. Вернуть итоговую оценку и валидность

---

### Операция: generateRecommendations

**Входные параметры:**
- `userId`: string

**Возвращает:**
- `recommendations`: Array<{
    - `project`: Project
    - `score`: number
    - `justification`: string
  }>

**Алгоритм:**
1. Получить профиль пользователя (технологии, CV, история)
2. Получить все активные проекты
3. Для каждого проекта:
   - Вычислить релевантность (calculateRelevanceScore)
   - Сгенерировать обоснование
4. Отсортировать по убыванию релевантности
5. Вернуть топ-N рекомендаций

---

### Операция: getSmartStack

**Входные параметры:**
- `projectId`: string

**Возвращает:**
- `recommendedStack`: Array<{
    - `technology`: TechnologyTag
    - `reason`: string
    - `strength`: number
  }>

**Алгоритм:**
1. Получить всех принятых кандидатов проекта
2. Извлечь технологии всех кандидатов с грейдами
3. Найти наиболее сильные технологии (высокие грейды у многих кандидатов)
4. Найти технологии для обучения (низкие грейды, но интерес к изучению)
5. Оптимизировать стек для баланса силы и обучения
6. Вернуть рекомендуемый стек с обоснованием

